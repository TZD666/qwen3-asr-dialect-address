---
type: spec
title: 评测体系设计 v2
description: 在不微调前提下，把 eval/run_eval.py 从"一个总分"扩成分阶段、分片、带负样本与 oracle 上限的评测体系。每一步附验证条件，验证不过不得进入下一步。
repo: TZD666/qwen3-asr-dialect-address
timestamp: 2026-09-10
---

# 评测体系设计 v2

## 0. 这套评测要回答什么

不微调，ASR 的错误分布就是固定输入，能改的只有后处理：地址库、代价矩阵、五个权重、三道阈值、上下文注入、n-best。评测体系因此只需回答两个问题：

1. 后处理在这个固定错误分布上的**净收益**是多少（纠对减去改坏）。
2. 后处理每个环节的**天花板**各有多高，从而决定先改哪一个。

现有 `run_eval.py` 只输出端到端字段级 EM、严重性分级、按 difficulty 分层。它能说明后处理没把对的改坏，说明不了上面两件事。原因有三个，对应本文三块改动：

| 缺口 | 后果 | 本文对应章节 |
|---|---|---|
| 没有负样本 | 测不出改坏率、过度纠正、注入被带偏 | §2 |
| 没有拆阶段 | 一条错例分不清坏在音变、库、排序还是闸门 | §3、§5 |
| 指标与损失不对齐 | 报准确率，但决策问题是错送率对自动通过率 | §4 |

五条原则贯穿全文：

- 每条样本都带分片标签，每个指标都按分片报，总平均只作参考。
- 负样本是评测集的必要组成，不是附加项。
- 评测集、校准集、权重学习集物理分开，标注时就分好。
- n < 20 的分片只报计数，不报百分比。
- 库、代价矩阵、权重、阈值任一改动都跑回归，任一分片退化即阻断。

---

## 1. 目录与文件约定

```
data/eval/
  xinan_guanhua.json          # 保留，item schema 按 §2.1 扩展
  manifest.jsonl              # 新增：所有音频的统一清单（§2.2）
  negatives/
    oov_drop.json             # 新增：库缺负样本——评测时从库里临时删除的条目（§2.3）
    injection.json            # 新增：注入负样本——由 reports/context_injection.json 固化（§2.3）
  synthetic/
    perturbed.jsonl           # 新增：按音变规则合成的文本扰动集（§2.4）
    gen_config.json           # 生成参数与随机种子
  splits.json                 # 新增：eval / calib / train 划分（§2.5）
  audio/
    recordings/               # 保留
    _smoke/                   # 保留

eval/
  run_eval.py                 # 扩展：分阶段指标、归因、oracle、配对（§3–§6）
  stages.py                   # 新增：六个阶段的打分函数，纯函数，可单测
  attribution.py              # 新增：错误归因决策树（§5）
  regression.py               # 新增：无模型回归套件（§7）
  golden/                     # 新增：回归基线快照
  reports/                    # 保留，报告格式按 §8

tests/
  test_normalize.py           # 保留
  test_stages.py              # 新增：每个阶段函数的单测
  test_attribution.py         # 新增：归因决策树的单测

.github/workflows/eval.yml    # 新增：CI 只跑无模型部分（§7）
```

---

## 2. 数据层

### 2.1 评测集 item schema 扩展

在现有 `xinan_guanhua.json` 每个 item 上新增字段。现有字段（`id / city / spoken / ground_truth / fields / difficulty / notes`）全部保留，`difficulty` 继续作为分片轴之一。

```json
{
  "id": "001",
  "dialect_group": "西南官话",
  "sub_dialect": "成都话",
  "address_depth": "full",
  "noise": "clean",
  "orthography_expected": "simplified",
  "gold_in_db": true,
  "negative_type": null,
  "split": "eval"
}
```

| 字段 | 取值 | 说明 |
|---|---|---|
| `dialect_group` | 官话 / 粤 / 闽 / 吴 / 湘 / 赣 / 客 | 对应 `romanize.py` 的音系路由，不用模型的 22 个标签 |
| `sub_dialect` | 自由文本 | 成都话、天津话等 |
| `address_depth` | `full` / `district` / `street_only` | 说话人给到哪一层。`street_only` 就是第 9 节那种不提省市的 |
| `noise` | `clean` / `filler` / `complaint` | 朗读稿是 clean；自发口语按有无口头禅、有无整段无关内容分 |
| `orthography_expected` | `simplified` / `traditional` / `cantonese_written` | 模型对这条录音预期输出的正字法 |
| `gold_in_db` | bool | 真值的最深层条目是否在 `cn_subset.json` 里。**由脚本自动计算**，不手填 |
| `negative_type` | `null` / `oov_db` / `asr_correct` / `injection` | 见 §2.3 |
| `split` | `eval` / `calib` / `train` | 见 §2.5 |

`gold_in_db` 的计算：取 `fields` 里最深的非空层级（community > road > street），在 `AddressDB` 里做精确名或别名查找。查不到即 `false`。

### 2.2 音频清单 manifest.jsonl

现在 43 条录音的问题是没有对齐标注。补一个清单，每行一条音频：

```json
{
  "file": "recordings/20260909_212400_auto.wav",
  "set": "spontaneous",
  "item_id": null,
  "speaker_id": "spk01",
  "dialect_group": "官话",
  "sub_dialect": "成都话",
  "transcript_gold": "哎呀真的烦你能不能给我寄到那个花牌坊交通巷三十八号碧园公寓",
  "address_gold": "四川省成都市金牛区花牌坊街交通巷38号碧园公寓",
  "fields_gold": { "province": "四川省", "city": "成都市", "district": "金牛区", "street": "花牌坊街", "community": "交通巷", "house_no": "38号" },
  "address_depth": "street_only",
  "noise": "complaint",
  "orthography_expected": "simplified",
  "gold_in_db": true,
  "split": "eval",
  "quality": "ok"
}
```

| 字段 | 说明 |
|---|---|
| `set` | `read`（按录音指南朗读 24 句，`item_id` 指向评测集）或 `spontaneous`（自发口语，`item_id` 为空） |
| `transcript_gold` | 说话人实际说的话，**逐字**，含口头禅和抱怨。这是算裸 ASR 的字级 CER 和拼音级 CER 的参照，不能用规范地址代替 |
| `address_gold` / `fields_gold` | 规范地址和结构化字段，格式同评测集 |
| `quality` | `ok` / `silent` / `clipped` / `unknown_format`。现有 5 条静音和 `.bin` 标进去，评测时跳过但保留 |

**标注规则：** `transcript_gold` 和 `address_gold` 由说话人本人写，不能事后凭听力标。原因是听录音标注的人会被 ASR 输出带偏，把"高通巷"听成真的。43 条录音都是作者本人的，这一条现在就能满足。

### 2.3 三类负样本

现有评测集没有任何一条会让后处理出错。三类负样本各测一种失效，构造成本都不需要新录音。

**(a) 库缺负样本 `oov_drop.json`。** 测过度纠正——正确答案不在候选集时，系统会不会把它"纠"成一个错的。

```json
{
  "description": "评测时从 AddressDB 临时删除的条目。被删条目对应的评测项自动获得 negative_type=oov_db。",
  "drop": [
    { "name": "交通巷", "parent": "花牌坊街", "affects": ["003", "009"] },
    { "name": "劝业场", "parent": "和平区", "affects": ["017"] }
  ]
}
```

`run_eval.py --negatives oov` 加载时先删这些条目再跑。选择原则：每个 `dialect_group` 至少 5 条，优先删那些库里有近音邻居的（中华中路旁边有中华北路那种），因为这是最容易被错纠的情形。

期望行为：这些项的 decision 应为 `reject` 或 `partial`，原文保留。若输出 `confident` 且地名被改，记为**过度纠正**。

**(b) ASR 已对负样本。** 测改坏率——裸 ASR 已经把地名听对了，后处理会不会反而改错。

不需要单独构造。`--mode audio` 跑完后，脚本对比 baseline 转写与 `transcript_gold`：地名片段（`fields_gold` 里 street/road/community 的值）在 baseline 输出里已经原样出现的，自动打上 `negative_type=asr_correct`。这个子集上的任何改动都是改坏。

**(c) 注入负样本 `injection.json`。** 把 `reports/context_injection.json` 那六种条件固化成回归用例，每条含音频、注入内容、期望关键字段。

```json
{
  "cases": [
    { "audio": "_smoke/tts_mandarin.wav", "inject": null, "expect_key": "红牌楼" },
    { "audio": "_smoke/tts_mandarin.wav", "inject": ["四川省成都市武侯区红牌坊街道", "四川省成都市金牛区花牌坊街"], "expect_key": "红牌楼", "known_flip": true }
  ]
}
```

`known_flip: true` 标记的是当前已知会被带偏的条件。回归时它们仍要跑，翻转数不得增加；如果哪次修改让它们不再翻转，就把 `known_flip` 去掉。这样第 13 节那张表变成了可追踪的技术债，而不是一段文字。

### 2.4 合成扰动集

文本层可以用 `pinyin_dialect.py` 里的 20 条声母规则和 26 条韵母规则反向生成错字：对 24 句朗读稿的地名片段，按规则挑同音或近音字替换，每句生成 20–50 个变体，总量到千级。

```json
{ "source_id": "003", "rule": "见系不颚化", "spoken": "送到花牌坊高通巷三十八号", "ground_truth": "...", "applied": [{"from": "交", "to": "高", "pinyin_from": "jiao", "pinyin_to": "gao"}] }
```

**这个集合有明确的适用边界。** 它是用自己的代价矩阵生成的，所以只能测排序、闸门、门牌解析和补全这些**下游**环节，不能测音距离本身——用代价矩阵生成的扰动来验证代价矩阵是循环论证。生成脚本和报告都要写明这一点。

### 2.5 三份划分

`splits.json` 按 `item_id` 和 `manifest` 的 `file` 划分。规则：

- 43 条自发录音全部进 `eval`，直到自发录音超过 150 条再分。
- 朗读集录齐后，按说话人分：同一说话人的录音不跨集。
- `calib`（保序回归）和 `train`（权重学习）在各自样本量到 §2.6 的门槛前**为空**。为空就不跑校准和权重学习，报告里写"未达门槛"，不用小样本凑。

### 2.6 样本量门槛

要对外说一个错送率，得知道这个数的置信区间。零错误时 95% 上限约为 3/n（rule of three），有错误时用 Wilson 区间。

| 想说的话 | 每个分片需要 |
|---|---|
| 错送率 ≤ 2%（观测到 0 错） | n ≥ 150 |
| 错送率 ≤ 1%（观测到 0 错） | n ≥ 300 |
| 分片百分比可报 | n ≥ 20 |
| 保序回归校准 | calib ≥ 300，且 confident 与非 confident 各不少于 50 |
| 权重学习（五个权重） | train ≥ 500，各 difficulty 标签 ≥ 30 |

现有 24 + 43 条：能跑通整套流程，能出归因分布和 oracle 提升，不能出任何带置信区间的数。报告里所有百分比旁边都带 Wilson 区间，n < 20 只显示 `k/n`。

按缺口补录的清单由报告自动生成（§8 的"样本缺口"节）：哪个 `dialect_group × address_depth × noise` 格子还差多少条。

---

## 3. 六个阶段的指标

流水线在 `pipeline.py` 里已经保留了每一步的中间结果（`pass1 / pass2 / RankResult / all_hits / nbest`）。评测按这些中间结果切成六段，每段一个打分函数，放在 `eval/stages.py`。

### Stage A 裸 ASR

只在 `--mode audio` 和 `--mode baseline` 有效，参照是 `transcript_gold`。

| 指标 | 定义 | 用途 |
|---|---|---|
| `cer_char` | 字级 CER | 现有 |
| `cer_phon` | 在路由后的音系空间（拼音 / 粤拼 / 台罗）算音节级 CER，用 `romanize.py` 转换两侧 | 新增 |
| `cer_name_char` / `cer_name_phon` | 只对地名片段算的字级与音级 CER | 新增 |
| `recoverable_share` | `(cer_name_char − cer_name_phon) / cer_name_char` | **整条路线在该分片的天花板** |

`recoverable_share` 高，说明错在字不在音，拼音匹配有得救；低，说明声学层就错了，后处理救不回来，这个分片要考虑微调或换音系库。吴湘赣用普通话近似的效果好不好，看这一个数。

### Stage B 归一化与抽取

参照是 `fields_gold` 的 TAIL 四字段和 `transcript_gold`。

| 指标 | 定义 |
|---|---|
| `lexicon_hit` | 方言词归一是否正确（"屋头"→"家"这类，闭集查表，逐条对） |
| `number_ok` | 口语数字转换正确（"三十八"→38，"幺零幺"→101） |
| `tail_exact` | 门牌/楼栋/单元/室四字段精确匹配，现有 |
| `illegal_number_caught` | 非法序列（"八百一百八"）是否被判非法并留空，而不是猜一个数 |
| `filler_leak` | 输出地址串或 `unverified` 字段里出现了 `transcript_gold` 中的非地址字（"巴适的板"这类）。按字符数计比例 |

### Stage C 召回

参照是真值链。定义**真值链**：从 `fields_gold` 反查 `AddressDB` 得到的 `Chain`（`gold_in_db=false` 时真值链为空，该项不参与 C/D 阶段，直接归因 `db_missing`）。

| 指标 | 定义 |
|---|---|
| `recall_hit` | 真值链最深层条目是否出现在 `RankResult.all_hits` |
| `recall_chain@K` | 真值链是否出现在 `nbest[:K]`，K = 1, 3, 全部 |
| `miss_reason` | 未召回时的原因：`dist_over_max`（音距离 > 0.40）/ `weak_filtered`（两音节弱命中被过滤）/ `window_miss`（滑窗未对齐）。三选一，由 `find_hits` 的中间量判定 |

召回失败和排序失败必须分开计。中华中路案例是召回失败（`db_missing`），不是排序失败；混在一起会把库的问题记到权重头上。

### Stage D 排序

只在 `recall_chain@all = true` 的子集上算。

| 指标 | 定义 |
|---|---|
| `top1_correct` | `nbest[0]` 是真值链 |
| `mrr` | 真值链名次的倒数 |
| `dominant_term` | 排序错误时，比较真值链与被选链的五项分数差 `Δsim / Δcov / Δprior / Δdepth / Δconflict`（各乘权重），绝对值最大的那项 |

`dominant_term` 的分布直接指向该改哪个权重或哪条规则。渝中区那次是 `conflict`，澳门那次是弱命中规则压了 `depth`。

### Stage E 闸门与决策

这是和损失对齐的核心。每条样本落入四格之一：

| | Top-1 正确 | Top-1 错误 |
|---|---|---|
| 自动通过（`confident`） | **TP** 有效通过 | **FA** 错送（最危险） |
| 拦截（`ambiguous` / `partial` / `reject`） | **FR** 多余确认 | **TR** 有效拦截 |

| 指标 | 定义 |
|---|---|
| `coverage` | (TP + FA) / n，自动通过率 |
| `risk` | FA / (TP + FA)，错送率 |
| `over_reject` | FR / (TP + FR)，本来对却拦下的比例 |
| `nbest_recall@3` | 被拦截且返回 nbest 的样本里，真值在前 3 的比例。这个数低，让用户确认就是空转 |
| `gate_attrib` | FA 或 FR 时，是哪道闸门放行/拦截的：`margin` / `sim` / `tail` |

**risk-coverage 曲线：** 对 `MARGIN_MIN ∈ [0.02, 0.20]`、`SIM_MIN ∈ [0.50, 0.80]` 网格扫描，每个点算一对 (coverage, risk)，输出曲线和当前阈值所在的点。损失比 `L(错送)/L(确认)` 定了之后，最优工作点从曲线上读，不再手拍。

**校准：** 把 `Chain.total` 分十个桶，每桶算实际正确率，画 reliability 图，报 ECE。`calib` 集为空时只画图不做保序回归。现在"24/24 全部 confident"这件事在 reliability 图上会表现为只有一个桶有数据——图本身就在说明校准不可测。

### Stage F 补全

只在 `address_depth ∈ {district, street_only}` 的子集上算。

| 指标 | 定义 |
|---|---|
| `backfill_correct` | 回溯出的省/市/区与 `fields_gold` 一致 |
| `wrong_hit_wrong_city` | 街巷命中错 → 回溯出错误城市的比例。这类错误整条链全错，后果比其他错重 |
| `backfill_confidence_gap` | 回溯层与命中层的置信度是否分开报（检查 `reason` 字段是否区分） |

### 端到端

保留现有 `exact / admin_all / geo_recall / tail_all / severity`。新增两项：

**投递等价 `deliverable`。** 省市区街路门牌全对，或路名不同但指向同一 POI（需要 POI 接口，接入前按精确匹配算）。

> 待议：`rank.py` 第一道闸门"Top1/Top2 仅指向同区不同道路不拦截"与投递等价的定义矛盾——同区不同路在投递上就是两个地方。评测按投递等价判对错后，这条规则的合理性会被数据暴露出来；本文不改它，只标记。

**纠正四格。** 对同一条音频，比较 baseline 与 audio 两种模式：

| | baseline 对 | baseline 错 |
|---|---|---|
| audio 对 | 保持 | **纠对** |
| audio 错 | **改坏** | 漏纠 |

净收益 = 纠对 − 改坏。改坏率是不微调路线特有的风险，因为微调不会把已经对的字改错，后处理会。

---

## 4. 分片轴

每个指标都按下面五个轴分别报。轴之间取笛卡尔积的格子用于"样本缺口"表，指标报告只按单轴分。

| 轴 | 来源 | 取值 |
|---|---|---|
| 音系 | `dialect_group` | 官话 / 粤 / 闽 / 吴 / 湘 / 赣 / 客 |
| 地址深度 | `address_depth` | full / district / street_only |
| 口语噪声 | `noise` | clean / filler / complaint |
| 难点 | `difficulty` | 现有 11 个标签，多标签 |
| 负样本类型 | `negative_type` | null / oov_db / asr_correct / injection |

另加一个派生轴 **短名**：真值链最深层名称 ≤ 2 音节的样本。澳门、台湾、鼓楼都落在这里，专门看弱命中规则的误伤。

---

## 5. 错误归因

每条端到端不对的样本贴**一个且只贴一个**标签。用决策树按固定顺序判，先命中先停，这样标签互斥。放在 `eval/attribution.py`。

```
1. quality != ok                              → skip（不计入）
2. gold_in_db == false                        → db_missing
3. Stage A: 地名片段音距离 > max_dist(0.40)    → asr_unrecoverable
      （在路由后的音系空间算，真值地名 vs ASR 输出对应片段）
4. Stage B 任一指标失败，且 C/D/E 均正确        → normalize_error
5. Stage C: recall_hit == false               → recall_miss（附 miss_reason）
6. Stage D: top1_correct == false             → rank_error（附 dominant_term）
7. Stage E: FA（Top-1 错但 confident）         → gate_over_accept（附 gate_attrib）
   Stage E: FR（Top-1 对但拦截）               → gate_over_reject（附 gate_attrib）
8. Stage F: backfill_correct == false         → completion_error
9. 以上都过但端到端仍错                         → assembly_error（拼装/字段映射问题，需人工看）
```

顺序的理由：越靠上的原因越"上游"，上游错了下游的错是它的传导，不该重复计。`db_missing` 排在 `asr_unrecoverable` 前面，因为库缺时后面所有阶段都没有参照。

报告输出归因分布，按分片。这张分布表是给 `docs/方案推演.md` 第 15 节六条优化方向排序的依据。目前 43 条里三个反面案例分别是 `db_missing`（中华中路）、`normalize_error`（巴适的板）、`rank_error`（澳门），但没有统计。

`tests/test_attribution.py` 要覆盖每个标签至少一条构造用例，并验证互斥性：任意一条样本跑决策树只能得到一个标签。

---

## 6. Oracle 实验

三个开关，各自把某一环节换成"完美"，看端到端提升多少。提升量就是那个环节的头部空间。

| 开关 | 做法 | 回答的问题 |
|---|---|---|
| `--oracle db` | 跑之前把所有 `gold_in_db=false` 项的真值条目临时插入 `AddressDB` | 接高德 POI 的收益上限 |
| `--oracle rank` | `recall_chain@all=true` 时强制选真值链为 Top-1，其余不变 | 权重学习的收益上限 |
| `--oracle gate` | Top-1 正确 → `confident`，否则 → `ambiguous` | 阈值调优的收益上限 |

三个 oracle 可以叠加（`--oracle db,rank`），输出一张表：

| 配置 | EM | deliverable | coverage | risk |
|---|---|---|---|---|
| baseline（裸 ASR + 归一化） | | | | |
| current | | | | |
| oracle db | | | | |
| oracle rank | | | | |
| oracle gate | | | | |
| oracle db+rank+gate | | | | |

最后一行和 current 的差就是后处理还能榨出来的全部空间；最后一行和 100% 的差就是 `asr_unrecoverable` + `normalize_error` + `completion_error`，后处理够不着的部分。

**单调性检查：** 任一 oracle 在任一分片上的指标都不得低于 current。低了说明 oracle 实现有 bug 或 current 有随机性，先修再看数。

---

## 7. 回归套件与阻断规则

`eval/regression.py` 不加载模型，跑三样东西：24 句 text 模式、合成扰动集、`oov_drop` + `injection` 负样本（注入用例需要模型，CI 里跳过，本地跑）。

基线快照存 `eval/golden/`，内容是上一次通过的报告 JSON。每次运行与快照逐分片比较，**任一条件触发即失败**：

- 任一分片的 `exact` 或 `deliverable` 下降
- `oov_db` 子集的过度纠正数上升
- `asr_correct` 子集的改坏数上升
- `injection` 用例中翻转数上升（`known_flip` 的翻转不算新增）
- 归因分布中 `assembly_error` 出现（意味着拼装逻辑改坏了）

以下文件任一改动都必须跑回归：`data/addresses/`、`pinyin_dialect.py`、`romanize.py`、`dialect_lexicon.py`、`normalize.py`、`rank.py`、`pipeline.py`。`.github/workflows/eval.yml` 对这些路径设 trigger，只跑无模型部分。

快照更新是显式动作（`regression.py --update-golden`），并要求在 commit message 里写明哪个分片为什么变。

---

## 8. 报告格式

每次运行输出一份 `.md` 和一份 `.json`，`.md` 固定六节，顺序不变，便于前后对比：

1. **总览**：n、模式、分片轴各有多少样本、样本缺口表（哪个格子 < 20）
2. **阶段指标**：A–F 六段，每段一张表，行是分片，列是指标，每个百分比带 Wilson 区间，n < 20 只显示 `k/n`
3. **决策四格与 risk-coverage**：四格计数、曲线数据、当前阈值所在点、reliability 图数据
4. **纠正四格**：仅 audio 模式，配对 baseline
5. **归因分布**：按分片，附每条错例的标签和关键中间量（ASR 片段、真值、音距离、dominant_term）
6. **Oracle 表**：§6 的六行表
7. **与上次对比**：自动读 `golden/` 或指定的上一份报告，逐分片列出变化 ≥ 1 个样本的项

`.json` 保留每条样本的全部中间量，供人工翻查和后续权重学习。

---

## 9. 配对比较与统计

- baseline 与 audio 在**同一批音频**上配对，净收益的置信区间用 bootstrap（1000 次重采样，按样本重采）。
- 两个版本的端到端对错用 McNemar 检验，报 p 值。n 小的时候 p 值没有意义，报告里同样按 n < 20 规则处理。
- 分片看，不看总量。修澳门那次如果改成按拼音碰撞数算区分度，要证明它没有反过来把 `gulou`、`xiangcheng` 这类高碰撞短名放行——短名分片和 `oov_db` 分片一起看。

---

## 10. 实施顺序与验证条件

每一步有验证条件，**验证不过不得进入下一步**。前四步不需要新录音，第五步需要模型，第六步不需要模型，第七步的机制不需要门槛、落地需要数据达到 §2.6 门槛，第八步不需要模型。

### 步骤 1：schema 与清单

做：§2.1 字段扩展；`gold_in_db` 自动计算脚本；§2.2 manifest，43 条由作者本人填 `transcript_gold` 和 `address_gold`；`splits.json` 初版（全部 eval）。

验证：
- `python eval/check_manifest.py` 通过：`recordings/` 下每个文件都有清单行；`quality=ok` 的行 `transcript_gold` 和 `address_gold` 非空；`gold_in_db` 与脚本重算一致。
- 现有 `--mode text` 仍为 24/24。

### 步骤 2：阶段指标与归因（文本模式）

做：`eval/stages.py` 六个打分函数；`eval/attribution.py` 决策树；`run_eval.py` 接入，报告按 §8 输出前五节。

验证：
- `tests/test_stages.py`、`tests/test_attribution.py` 通过，归因互斥性用例通过。
- 故意破坏测试：从库里删 5 条评测集用到的条目跑 text 模式，这 5 条必须全部归因为 `db_missing`，其余 19 条仍 exact。
- 故意破坏测试：把 `SIM_MIN` 临时改到 0.95 跑 text 模式，所有失败项必须归因为 `gate_over_reject`。

### 步骤 3：负样本与合成集

做：§2.3 的 `oov_drop.json`、`injection.json`；§2.4 生成脚本与 `perturbed.jsonl`；`run_eval.py --negatives` 开关。

验证：
- `--negatives oov` 跑出来过度纠正数 > 0（如果是 0，说明 drop 列表选的条目没有近音邻居，换）；且每条过度纠正都归因 `db_missing`。
- 合成集 ≥ 500 条，每条 `applied` 字段可回放（替换字的拼音确实落在所用规则的混淆对上）。
- `injection.json` 跑一遍，结果与 `reports/context_injection.json` 六条逐一相同。

### 步骤 4：Oracle

做：§6 三个开关。

验证：
- 单调性：每个 oracle 在每个分片上不低于 current。
- `--oracle db,rank,gate` 在 text 模式下应为 24/24（text 模式无 ASR 错误，三个 oracle 叠加后不该有剩余错误；若有，归因必为 `normalize_error` 或 `assembly_error`，逐条解释）。

### 步骤 5：音频配对与 risk-coverage

做：`--mode audio` 与 `--mode baseline` 配对输出纠正四格；Stage A 的 `cer_phon` 与 `recoverable_share`；阈值网格扫描；bootstrap 与 McNemar。

验证：
- 43 条中 `quality=ok` 的每条都有 baseline 与 audio 两个结果，配对无缺失。
- risk-coverage 曲线上 coverage 随 `MARGIN_MIN` 增大单调不增，随 `SIM_MIN` 增大单调不增。
- 当前阈值 (0.06, 0.62) 作为一个点出现在曲线上，其 (coverage, risk) 与报告第 3 节四格计数一致。

### 步骤 6：回归与 CI

做：`eval/regression.py`、`eval/golden/` 初始快照、`.github/workflows/eval.yml`。

验证：
- 在干净仓库上跑 `regression.py` 通过。
- 故意改坏测试：把 `W_CONFLICT` 改成 1.0 提交，CI 必须红；改回来，CI 必须绿。

### 步骤 7：调参闭环（机制先建，落地等门槛）

原稿写的是"数据达门槛后再做校准与权重学习"。改成：**闭环现在就建，每次都跑，只是未达门槛时只出提案不落地**。一个只在想象里存在的调参流程，和没有一样。

做：
- 参数出代码进数据：`data/params/rank_params.json`，带 `version / source(hand-set|tuned|tuned-below-threshold) / created / data_snapshot / history`。`rank.py` 导入时加载，模块属性名不变。服务 `/api/status` 与每份报告头报参数版本和代码提交号。
- 特征重放：`eval/features.py` 对每条样本只跑一次检索，存下全部候选链的五项分量、是否真值链、行政区键、深命中、孤证距离；`eval/rescore.py` 用任意 (权重, 阈值) 毫秒级重算总分并重走三道闸门，与 `rank.decide` 同一顺序。
- 目标与约束：`data/params/tune_config.json` 定代价比（c_fa : c_fr 默认 10 : 1）；`J = 训练片平均代价 + 0.001·‖θ − θ_hand‖₁`，平台上偏向离手设值最近的点。硬约束在留出片上查：EM 不降、risk 不升、`oov_db` 过度纠正不升、无地址错送不升、n ≥ 20 的分片不丢 TP、回归通过。
- 划分：`data/eval/splits.json` 唯一来源；合成集按 `source_id` 分组不跨集；有效样本按句计（30 个变体 = 1）。门槛 train_eff ≥ 500 且各 difficulty ≥ 30 才允许落地权重与阈值；calib_eff ≥ 300 且两类各 ≥ 50 才存校准表。
- 搜索：坐标下降 + 随机重启，权重在 simplex 上等比缩放，阈值按步长扫。输出**可辨识表**：每个参数在最优点附近目标不变的区间——数据钉不住的参数，闭环不动它。
- 校准：`eval/calibrate.py` 纯 Python 保序回归，报校准前后 ECE 与损失比隐含的 p*；本轮只存表只展示，不接进闸门。
- 命令：`eval/features.py` → `eval/tune.py [--confirm]` → 提案 `eval/tuning/<时间>_proposal.{json,md}` → `eval/tune.py --apply <提案>` 写新版本 → `eval/regression.py --update-golden`。**apply 永远是本地显式命令**，CI 只产提案作为 artifact。
- 新数据接入：`scripts/intake.py`：转写进缓存 → 补清单行 → 说话人填真值 → 校验 → 特征 → 提案。

验证：
- 特征重放在手设参数下与现场决策逐条一致（`tests/test_features.py`）。
- 玩具集上搜索找回植入的最优；目标平时提案与手设一致；约束器拒绝降 EM 的提案（`tests/test_tune.py`）。
- `--confirm` 报真跑四格与重放四格的偏差，且回归通过；提案 verdict 只能是 REPORT_ONLY / APPLICABLE / REJECTED 三者之一。
- 校准后 ECE 不高于校准前。
- 学到的参数在 `eval` 片上 EM 与 risk 不劣于手设，`oov_db` 过度纠正不上升（约束表逐条 ✓）。

### 步骤 8：评测自检（"这套评测够不够全面"要有证据）

做：`eval/selfcheck.py` 对每个归因标签构造一种故意破坏（改参数、删库条目、改真值），断言评测贴出预期标签；再汇总样本缺口与标注状态、合成规则覆盖、真实数据上出现过的标签、参数可辨识性，收尾一张"能抓住什么、抓不住什么、需要什么数据"。声明可达却没贴出标签 → 非零退出；声明不可达的标签跑"最像它"的情形，确认记到了真因上。

验证：
- 每个归因标签至少一个场景；`W_CONFLICT=1.0` 让回归变红，`SIM_MIN` 越过 1 才能在朗读稿上拦下（文本数据相似度全为 1.0，这是自检要写出来的事实，不是失败）。
- CI 里 `--fast --ci` 跑通；`tests/test_selfcheck.py` 覆盖文本类场景。

---

## 11. 现在就能做的最小闭环

用现有 24 + 43 条，走完步骤 1–4，产出三样东西：

1. 一张 43 行的表，每行有裸 ASR、真值、决策、逐层对错、归因标签。
2. 归因分布：四种错各占多少。
3. Oracle 表：库完备、排序完美、闸门完美各能推到多少。

这三样都不能对外报，但足够决定第 15 节六条方向里先做哪条。录音则从"多录一些"变成按报告第 1 节的样本缺口表补，200–300 条分到几个说话人，每人 30 句左右。
